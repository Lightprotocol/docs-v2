---
title: "Light Protocol"
mode: "custom"
---

<div style={{
  position: 'relative',
  minHeight: '100vh'
}}>
  {/* Vertical lines at 20% and 80% - full height */}
  <div style={{
    position: 'absolute',
    left: '20%',
    top: 0,
    bottom: 0,
    width: '2px',
    backgroundImage: 'repeating-linear-gradient(to bottom, rgba(128,128,128,0.3) 0, rgba(128,128,128,0.3) 12px, transparent 12px, transparent 20px)'
  }} />
  <div style={{
    position: 'absolute',
    right: '20%',
    top: 0,
    bottom: 0,
    width: '2px',
    backgroundImage: 'repeating-linear-gradient(to bottom, rgba(128,128,128,0.3) 0, rgba(128,128,128,0.3) 12px, transparent 12px, transparent 20px)'
  }} />

  {/* Empty top space */}
  <div style={{ height: '60vh' }} />

  {/* Horizontal line - full width */}
  <div style={{
    height: '2px',
    backgroundImage: 'repeating-linear-gradient(to right, rgba(128,128,128,0.3) 0, rgba(128,128,128,0.3) 12px, transparent 12px, transparent 20px)'
  }} />

  {/* Three boxes */}
  <div style={{
    display: 'flex',
    margin: '0 20%'
  }}>
    <div style={{
      flex: 1,
      padding: '2rem',
      textAlign: 'center',
      borderRight: '2px dashed rgba(128,128,128,0.3)'
    }}>
      <h2>Light-Token</h2>
    </div>

    <div style={{
      flex: 1,
      padding: '2rem',
      textAlign: 'center',
      borderRight: '2px dashed rgba(128,128,128,0.3)'
    }}>
      <h2>c-PDA</h2>
    </div>

    <div style={{
      flex: 1,
      padding: '2rem',
      textAlign: 'center'
    }}>
      <h2>Compressed Tokens</h2>
    </div>
  </div>

  {/* Horizontal line - full width */}
  <div style={{
    height: '2px',
    backgroundImage: 'repeating-linear-gradient(to right, rgba(128,128,128,0.3) 0, rgba(128,128,128,0.3) 12px, transparent 12px, transparent 20px)'
  }} />

  {/* Fade out */}
  <div style={{
    height: '30vh',
    background: 'linear-gradient(to bottom, transparent, var(--background))'
  }} />
</div>
